package fundamentals.linear;

public class Task01 {
    public static void main(String[] args) {
        double x = 10;
        double y = -10.46;
        double sum = x + y;
        double subtract = x - y;
        double multiply = x * y;
        double divide = x / y;
        System.out.println("Sum = " + sum + "\nSubtract = " + subtract + "\n" +
                "Multiply = " + multiply + "\nDivide = " + divide);

    }
}
